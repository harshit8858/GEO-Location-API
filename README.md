# GEO-Location-API

It’s a simple API that provides geo location information (country, timezone, latitude, longitude, etc) based on IP addresses.

Requests consumed from http://freegeoip.net/json/

pip install requests
